<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="ValueView">
        <attr name="pattern" format="string" />
    </declare-styleable>

    <declare-styleable name="LabelView">
        <attr name="text" format="string" />
        <attr name="align" format="enum">
            <enum name="center" value="0" />
            <enum name="left" value="-1" />
            <enum name="right" value="1" />
        </attr>
    </declare-styleable>

    <declare-styleable name="ValueContainer">
        <attr name="field_low" format="boolean"/>
        <attr name="field_high" format="boolean"/>
        <attr name="field_target" format="boolean"/>
    </declare-styleable>

    <declare-styleable name="ChartView">
        <attr name="fill" format="color" />
        <attr name="stroke" format="color" />
        <attr name="ticks" format="color" />
    </declare-styleable>

    <declare-styleable name="SegmentsView">
        <attr name="drawable" format="reference" />
        <attr name="orientation" format="enum">
            <enum name="horizontal" value="0" />
            <enum name="vertical" value="1" />
        </attr>
    </declare-styleable>

</resources>
